import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.anses.miapp.push',
  appName: 'AnsesNoti',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
